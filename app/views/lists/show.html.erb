

<% @list.movies.each do |movie| %>
<%= movie.title %>
<%= movie.overview %>
<%= movie.poster_url %>
<%= movie.rating %>
<% movie.bookmarks do |bookmark| %>
<%= bookmark.comment %>
<% end %>
<% end %>


<%= link_to  "Create bookmark", new_list_bookmark_path(@list) %>
<%# <%= link_to "Delete", bookmark_path(bookmark), action: :destroy, method: :destroy, data: {confirm: 'Are you sure you want to delete?'} %>
